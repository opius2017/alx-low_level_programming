#include <stdio.h>

/**
 * main - the function to find args
 * @argc: Argument count
 * @argv: Argument vector
 * Return:Always 0
 */

int main(int argc, char __attribute__((__unused__)) *argv[])
{
	printf("%d\n", argc - 1);

	return (0);
}
